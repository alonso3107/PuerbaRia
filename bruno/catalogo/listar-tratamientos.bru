meta {
  name: Listar tratamientos de spa
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/spa/tratamientos
  body: none
  auth: none
}

tests {
  test("devuelve tratamientos", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array").that.is.not.empty;
  });
}

docs {
  Endpoint publico. Devuelve los tratamientos del spa.
}
